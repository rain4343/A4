{
  "name": "A2",
  "private": true,
  "workspaces": [
    "artifacts/*"
  ],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "drizzle-kit": "^0.30.0"
  }
}